"""Build the provider dict. Placeholder until Task 3 adds the real registry."""
from __future__ import annotations

from ..config import Settings


def build_providers(settings: Settings) -> dict:
    return {}
